<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" crossorigin="anonymous" />
    <link rel="stylesheet" href="{{ asset('assets/css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="{{ asset('assets/css/home.css') }}">
   
</head>
<body>
<nav class="navbar navbar-expand-lg bg-light" data-bs-theme="light" style="height:90px">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ url('app_home') }}" style="background: linear-gradient(to right, #FF0080, #FF5F00); -webkit-background-clip: text; -webkit-text-fill-color: transparent;font-size : 30px; font-family:Times new roman">Bookland</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item" style="margin-left:50px">
                        <a class="nav-link " href="{{ url('app_livres') }}"><i class="fa fa-book" style="color: black;font-size:20px"></i><strong>Livres</strong>
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                                <li class="nav-item" style="margin-left:50px">
                <a class="nav-link" href="{{ url('app_list_avis') }}">
                    <i class="fas fa-comments" style="color: black;font-size:20px"></i>
                    <strong>Avis</strong>
                    <span class="visually-hidden">(current)</span>
                </a>
            </li>

        <li class="nav-item" style="margin-left:50px">
            <a class="nav-link" href="{{ url('app_categorie_index') }}">
                <i class="fas fa-list" style="color: black;font-size:20px"></i> 
                <strong>Catégories</strong>
            </a>
        </li>


        <li class="nav-item" style="margin-left:50px">
            <a class="nav-link" href="{{ url('app_dashboard') }}">
            <i class="fas fa-chart-bar" style="color: black;font-size:20px"></i>
                <strong>Dashboard</strong>
            </a>
        </li>
        <li class="nav-item" style="margin-left:50px">
            <a class="nav-link" href="{{ url('app_reclamation_list') }}">
            <i class="fas fa-exclamation-triangle" style="color: black;font-size:20px"></i>
                <strong>Réclamations</strong>
            </a>
        </li>
        <li class="nav-item" style="margin-left:50px">
            <a class="nav-link" href="{{ url('app_user_index') }}">
            <i class="fa fa-users" style="color: black;font-size:20px"></i>
                <strong>Utilisateurs</strong>
            </a>
        </li>
        <li class="nav-item" style="margin-left:50px">
                      <a class="nav-link " href="{{ url('list_commande') }}"><i class="fas fa-shipping-fast" style="color: black;font-size:20px"></i>
                         <strong>Commandes</strong> <span class="visually-hidden">(current)</span>
                      </a>
                  </li>

                {% endif %}
                {% if not is_granted('ROLE_ADMIN') %}
                <li class="nav-item" style="margin-left:50px">
                    <a class="nav-link " href="{{ url('app_list_livres') }}"><i class="fa fa-book" style="color: black;font-size:28px"></i>
                       <strong>Livres</strong> <span class="visually-hidden">(current)</span>
                    </a>
                </li>
                <li class="nav-item" style="margin-left:50px">
                    <a class="nav-link " href="{{ url('app_reclamation') }}"><i class="fas fa-exclamation" style="color: black;font-size:28px"></i>
                       <strong>Réclamation</strong> <span class="visually-hidden">(current)</span>
                       
                    </a>
                </li>
               {% endif %}
            </ul>
            <ul class="navbar-nav me-ml">
            {% if not is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ url('panier_afficher') }}"><i class="fa fa-shopping-cart" style="color: black;font-size:28px"></i>
                    <span class="badge">
                        
                        </span></a>
                </li>
                {% endif %}
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('app_logout') }}"><i class="fas fa-sign-out-alt" style="color: black;font-size:28px"></i></a>
                    </li>
                {% endif %}
                {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ url('app_register') }}"><strong>S'inscrire</strong>
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="{{ url('app_login') }}"><strong>Connexion</strong>
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
<div class="mt-3">

    {% block body %}
    {% endblock %}
</div>
</body>
</html>
